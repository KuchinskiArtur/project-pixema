export enum SidebarTab {
  Home = "Home",
  Trends = "Trends",
  Favorites = "Favorites",
  Settings = "Settings",
}

export enum Path {
  Home = "/home",
  Settings = "/settings",
  Favorites = "/favorites",
  Trends = "/trends",
  Login = "/login",
  Registration = "/registration",
}

export enum Tab {
  Movie = "Movie",
  Series = "Series",
}

export enum Theme {
  Light = "light",
  Dark = "dark",
}

export enum Filter {
  Movie = "Movie",
  Series = "Series",
}

export enum ButtonConstants {
  Clear = "Clear filter",
  Show = "Show results",
}
